/**
 * GraphQL Gateway — optional schema stitching for remote services discovered via service-registry.
 *
 * <p>Activate by setting {@code GRAPHQL_GATEWAY_ENABLED=true}. Only services with
 * {@code graphqlPath} metadata in the service registry are introspected and merged.</p>
 */
package com.guicedee.vertx.graphql.gateway;

